# The simplest example of moving around in vim

# Case 1: use `w` and `W`
value = [1, 2, 3]
other_value = SomeClass(parameters)

# Case 2: use `b` and `e` with the same code

# Case 3: go around with:
#         - h - left
#         - j - down
#         - k - up
#         - l - right

# Case 4: Jump over blocks using `{` and `}`

class MyFirstClassWrittenInPython():

    def __init__(self):
        pass

really.long().evaluation().of().elements()

# Case 5: use `^` for the begining of the line
#         and `$` for end of it.

# Case 6: search for word. Search for similar
#         and use regexp
